global:
  scrape_interval: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: schniffer
    static_configs:
      - targets: ['schniffer:9090']
        labels:
          service: schniffer
